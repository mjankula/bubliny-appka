apiVersion: v1
kind: Service
metadata:
  labels:
    app: bubliny-appka
  name: bubliny-appka
  namespace: namespace1
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: bubliny-appka
